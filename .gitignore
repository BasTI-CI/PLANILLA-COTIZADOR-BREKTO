node_modules
.pnpm-store
dist
.env.local
.env
*.local
.DS_Store
tsconfig.tsbuildinfo

# Lock npm si alguien corre `npm install`; el proyecto usa pnpm (pnpm-lock.yaml)
package-lock.json

# Local Netlify folder
.netlify
